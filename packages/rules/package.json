{
  "name": "@sortiva/rules",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "signals.config.yaml, the typed loader, and rules_version — the only place a threshold may live.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./*": "./src/*.ts"
  },
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "ajv": "^8.17.1",
    "yaml": "^2.6.1"
  },
  "devDependencies": {
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  },
  "files": [
    "src",
    "schema",
    "signals.config.yaml"
  ]
}
