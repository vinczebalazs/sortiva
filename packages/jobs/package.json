{
  "name": "@sortiva/jobs",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Graphile Worker tasks: one file per step/job.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./*": "./src/*.ts"
  },
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit"
  }
}
