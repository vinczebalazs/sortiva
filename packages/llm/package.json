{
  "name": "@sortiva/llm",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "The single instrumented Anthropic client, plus the versioned prompts, schemas and eval sets.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./*": "./src/*.ts"
  },
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.122.0",
    "ajv": "^8.17.1",
    "@sortiva/core": "workspace:*",
    "@sortiva/rules": "workspace:*"
  },
  "devDependencies": {
    "@sortiva/providers": "workspace:*",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
