{
  "name": "@sortiva/providers",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "shopify, gsc, seo, email, stripe, posthog — each behind an interface with a test double.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./*": "./src/*.ts"
  },
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit"
  }
}
